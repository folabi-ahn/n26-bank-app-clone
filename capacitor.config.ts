import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'n26-bank-app-clone',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
